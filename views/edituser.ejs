<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit user</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-begin">
            <a class="bg-zinc-700 rounded-3xl px-3 py-2 text-sm mb-5 inline-block" href="/profile/<%= user._id %>"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>
        </div>
        <h5 class="mb-5">Edit User Details.</h5>
        
        <form method="post" action="/updateuser/<%= user._id %>">
            <div class="relative">
                <div class="relative w-[200px] h-[200px] rounded-md overflow-hidden">
                    <img src="data:<%= user.profilepic.contentType %>;base64,<%= user.profilepic.data.toString('base64') %>" alt="Profile Picture">
                    <a class="bg-zinc-700 rounded-3xl px-3 py-2 text-sm absolute bottom-2 right-2 z-10" 
                       href="/edituser/<%= user._id %>/upload">
                        <i class="fa fa-pencil text-white" aria-hidden="true"></i>
                    </a>
                </div>
            </div>
            
            <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="text" placeholder="name" name="name" value="<%= user.name%>">
            <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="number" placeholder="age" name="age" value="<%= user.age%>">
            <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="password" placeholder="Old password" name="oldpassword">
            <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="password" placeholder="New password" name="newpassword">
            <div class="flex gap-8 mt-2">
                <input class="px-3 py-2 bg-yellow-500 rounded-md text-black block w-40" type="submit" value="Update user">
                <a class="px-3 py-2 bg-red-900 rounded-md w-40 h-30 flex justify-center inline-block" href="/deleteuser/<%= user._id %>">Delete User</a>
            </div>
        </form>     
    </div>
</body>
</html>