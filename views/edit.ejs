<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post - Postbook</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="container fade-in">
        <div class="nav">
            <a href="/dashboard" class="nav-back">
                <i class="fas fa-arrow-left"></i>
            </a>
        </div>
        
        <div class="card" style="max-width: 600px; margin: 0 auto;">
            <h1 style="font-size: 2rem; font-weight: 700; margin-bottom: 8px; background: linear-gradient(135deg, #3b82f6, #8b5cf6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                <i class="fas fa-edit" style="margin-right: 12px;"></i>
                Edit Post
            </h1>
            <p style="color: #9ca3af; font-size: 1.1rem; margin-bottom: 32px;">Update your post content</p>
            
            <form method="post" action="/update/<%= post._id %>" style="max-width: none;">
                <div class="form-group">
                    <textarea id="content" placeholder="What's on your mind?" name="content" class="form-input" required><%= post.content %></textarea>
                    <label for="content" class="form-label">Post Content</label>
                </div>
                
                <div style="display: flex; gap: 16px; flex-wrap: wrap; margin-top: 24px;">
                    <button type="submit" class="btn btn-warning">
                        <i class="fas fa-save"></i>
                        Update Post
                    </button>
                    <a href="/delete/<%= post._id %>" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this post? This action cannot be undone.')">
                        <i class="fas fa-trash"></i>
                        Delete Post
                    </a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>