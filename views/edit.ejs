<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit posts</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <h5 class="mb-5">Edit your post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's in your mind ?" class=" p-3 w-1/3 resize-none outline-none rounded-md bg-transparent border-2 border-zinc-800" name="content"><%= post.content %></textarea>
            <div class="flex gap-8 mt-2">
                <input class="px-3 py-2 bg-yellow-500 rounded-md text-black block w-40" type="submit" value="Update Post">
                <a class="px-3 py-2 bg-red-500 rounded-md w-40 h-30 flex justify-center inline-block" href="/delete/<%= post._id %>">Delete</a>
            </div>
        </form>     
    </div>
</body>
</html>