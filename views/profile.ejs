<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class ="flex">
            <div class="w-full flex justify-begin">
                <a class="bg-zinc-700 rounded-3xl px-3 py-2 text-sm mb-5 inline-block" href="/dashboard"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>
            </div>
            <div class="w-full flex justify-end gap-3">
                <a class="bg-yellow-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/edituser/<%= user._id %>">Edit user</a>
                <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Log out</a>
            </div>
        </div>
        <h3 class="text-3xl mb-5"><span class="font-light">Profile</h3>
        <div class="flex flex-col gap-3">
            <div class="w-[200px] h-[200px] rounded-md">
                <img src="<%= user.profilepic ? '/profilepic/' + user.profilepic : '/images/default.png' %>" alt="Profile Picture">
            </div> 
            <form method="get">
                <div class="flex flex-col gap-3 w-1/4">
                    <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="text" value="<%= user.name %>" name="name" readonly>
                    <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="text" value="<%= user.username %>" name="username"  readonly>
                    <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="email" value="<%= user.email %>" name="email" readonly>
                    <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="number" value="<%= user.age %>" name="age" readonly>
                </div>
            </form>   
        </div>
    </div>
</body>
</html>