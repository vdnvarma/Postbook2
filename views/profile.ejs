<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Postbook</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="container fade-in">
        <div class="nav">
            <a href="/dashboard" class="nav-back">
                <i class="fas fa-arrow-left"></i>
            </a>
            <div class="nav-actions">
                <a href="/edituser/<%= user._id %>" class="btn btn-warning">
                    <i class="fas fa-edit"></i>
                    Edit Profile
                </a>
                <a href="/logout" class="btn btn-danger">
                    <i class="fas fa-sign-out-alt"></i>
                    Logout
                </a>
            </div>
        </div>
        
        <div class="profile-header">
            <img src="data:<%= user.profilepic.contentType %>;base64,<%= user.profilepic.data.toString('base64') %>" alt="Profile Picture" class="profile-avatar">
            <div class="profile-info">
                <h1 class="profile-name"><%= user.name %></h1>
                <p class="profile-username">@<%= user.username %></p>
            </div>
        </div>
        
        <div class="card">
            <h2 style="font-size: 1.5rem; font-weight: 600; margin-bottom: 24px; color: #e5e7eb;">
                <i class="fas fa-user-circle" style="color: #3b82f6; margin-right: 8px;"></i>
                Profile Information
            </h2>
            
            <form method="get" style="max-width: 500px;">
                <div class="form-group">
                    <input type="text" id="name" value="<%= user.name %>" name="name" class="form-input" placeholder="Full Name" readonly>
                    <label for="name" class="form-label">Full Name</label>
                    <i class="fas fa-user form-icon"></i>
                </div>
                
                <div class="form-group">
                    <input type="text" id="username" value="<%= user.username %>" name="username" class="form-input" placeholder="Username" readonly>
                    <label for="username" class="form-label">Username</label>
                    <i class="fas fa-at form-icon"></i>
                </div>
                
                <div class="form-group">
                    <input type="email" id="email" value="<%= user.email %>" name="email" class="form-input" placeholder="Email Address" readonly>
                    <label for="email" class="form-label">Email Address</label>
                    <i class="fas fa-envelope form-icon"></i>
                </div>
                
                <div class="form-group">
                    <input type="number" id="age" value="<%= user.age %>" name="age" class="form-input" placeholder="Age" readonly>
                    <label for="age" class="form-label">Age</label>
                    <i class="fas fa-birthday-cake form-icon"></i>
                </div>
            </form>
        </div>
    </div>
</body>
</html>